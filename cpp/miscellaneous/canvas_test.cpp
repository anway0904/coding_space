#include <cmath>
#include <iostream>
#include <chrono>
#include <thread>

int main() {
    const int WIDTH = 500;
    const int HEIGHT = 500;
    
    std::cout << "\033[2J"; // Clear the screen
    std::cout << "\033[H";  // Move cursor to home position (top left)

    
    return 0;
}